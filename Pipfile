[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.8"

[packages]
freeholdforecast = {editable = true, extras = ["dev"], path = "."}

[scripts]
install-deps = "pipenv install -e '.[dev]'"
start-notebook = "jupyter notebook"
dbx-deploy = "dbx deploy freeholdforecast-etl-ml"
dbx-launch = "dbx launch freeholdforecast-etl-ml"
dbx-deploy-assets = "dbx deploy freeholdforecast-etl-ml --assets-only"
dbx-launch-assets = "dbx launch freeholdforecast-etl-ml --from-assets"
