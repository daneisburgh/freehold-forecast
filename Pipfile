[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[requires]
python_version = "3.8"

[packages]
freeholdforecast = {editable = true, extras = ["dev"], path = "."}

[scripts]
install-app = 'pipenv install -e ".[dev]"'
